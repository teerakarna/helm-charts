{{- if eq .Values.workloadType "job" -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "scoutsuite.fullname" . }}
  labels:
    {{- include "scoutsuite.labels" . | nindent 4 }}
  {{- with .Values.podAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  backoffLimit: {{ .Values.backoffLimit }}
  {{- if .Values.activeDeadlineSeconds }}
  activeDeadlineSeconds: {{ .Values.activeDeadlineSeconds }}
  {{- end }}
  template:
    metadata:
      labels:
        {{- include "scoutsuite.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- include "scoutsuite.podSpec" . | nindent 6 }}
{{- end }}
